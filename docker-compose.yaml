services:
  backend:
    build: ./backend
    image: backend
    command: uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/.venv
    environment:
      PYTHONUNBUFFERED: 1
